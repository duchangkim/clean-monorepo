import { designPeterpanDesignSystem } from './design-peterpan-design-system';

describe('designPeterpanDesignSystem', () => {
  it('should work', () => {
    expect(designPeterpanDesignSystem()).toEqual('design-peterpan-design-system');
  });
});
